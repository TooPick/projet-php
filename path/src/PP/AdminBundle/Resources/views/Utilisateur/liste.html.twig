{% extends "::layout.html.twig" %}

{% block body %}
	{% include "PPAdminBundle:Admin:navAdministration.html.twig" %}

	<form method="POST">
		<div class="input-group">
			<input class="form-control" type="text" name="searchUser" id="searchUser" placeholder="Rechercher un utilisateur...">
			<span class="input-group-btn">
				<input class="btn btn-default" type="submit" value="Rechercher" name="rechercher" id="rechercher">
			</span>
		</div>
	</form>
	<br />
	
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Nom d'utilisateur</th>
				<th>Email</th>
				<th>Nombre de recettes</th>
				<th>Nom</th>
				<th>Prénom</th>
				<th>Actions</th>
			</tr>
		</thead>
		
		<tbody>
			{% for utilisateur in utilisateurs %} 
				<tr>
					<td>{{ utilisateur.username }}</td>
					<td>{{ utilisateur.utiMail }}</td>
					<td>A faire !!</td>
					<td>{{ utilisateur.utiNom }}</td>
					<td>{{ utilisateur.utiPrenom }}</td>
					<td><button class="btn btn-primary" onclick="window.location.href='{{ path('pp_admin_editerUtilisateur', {id:utilisateur.id}) }}'"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editer</button> <button class="btn btn-primary" onclick="window.location.href='{{ path('pp_admin_listeRecettesUtilisateur', {id:utilisateur.id}) }}'"><span class="glyphicon glyphicon-list" aria-hidden="true"></span> Liste des recettes</button></td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
