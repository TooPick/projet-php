<nav>	
	<ul  class="nav nav-pills nav-stacked">
		<li>
		<form action="{{ path('pp_appli_search') }}" method="POST">
			<div class="input-group">
				<input class="form-control" type="text" name="search" id="search" placeholder="Rechercher...">
				<span class="input-group-btn">
					<input class="btn btn-default" type="submit" value="Rechercher" name="rechercher" id="rechercher">
				</span>
			</div>
		</form>
		<hr>
		<li>
		<li><a href="{{ path('pp_appli_index') }}">Accueil</a></li>
		{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
			<li><a href="{{ path('logout') }}">Déconnexion</a></li>
			<li class="dropdown">
				<a class="dropdown-toggle" data-toggle="dropdown" href="#">Mon compte <span class="caret"></span></a>
				<ul class="dropdown-menu">
					<li><a href="{{ path('pp_user_profil') }}">Mon profil</a></li>
					<li class="divider"></li>
					<li><a href="{{ path('pp_appli_ajouterRecette') }}">Ajouter une recette</a></li>
				</ul>
			</li>
		{% else %}
			<li><a href="{{ path('login') }}">Connexion</a></li>
			<li><a href="{{ path('inscription') }}">Inscription</a></li>
		{% endif %}
		<li class="dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#">Recettes <span class="caret"></span></a>
			<ul class="dropdown-menu">
				<li><a href="{{ path('pp_appli_recetteListe') }}">Liste des catégories</a></li>
				<li><a href="{{ path('pp_appli_nouveautes') }}">Nouveautés</a></li>
				<li><a href="">Populaires</a></li>
				<li class="divider"></li>
				<li><a href="{{ path('pp_appli_recetteListe', {idCat:1}) }}">Entrées</a></li>
				<li><a href="{{ path('pp_appli_recetteListe', {idCat:2}) }}">Plats</a></li>
				<li><a href="{{ path('pp_appli_recetteListe', {idCat:3}) }}">Desserts</a></li>
			</ul>
		</li>
		{% if is_granted('ROLE_ADMIN') %}
			<li><a href="{{ path('pp_admin_homepage') }}">Administration</a></li>
		{% endif %}
	</ul>
</nav>